This is a local tester for TypeSafe Jev through Vercel AI Gateway.

Put your AI Gateway key in `.env.local`:

```
AI_GATEWAY_API_KEY=your_key
```

Restart `pnpm dev` after changing the key. Open [http://localhost:3000](http://localhost:3000).
